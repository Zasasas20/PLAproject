## Setup

To setup the repo, first clone it
```git clone git@github.com:Zasasas20/PLAproject.git```

Then you need to setup the submodules:

```git submodule init```
```git submodule update --init --recursive```

This initializes the submodules, which are their own repositories.
